plugins {
    id 'java'
}

group 'com.alexanderrodnin.simplegraphlib'
version '0.0.1'

repositories {
    mavenCentral()
}

dependencies {

    compile 'one.util:streamex:0.7.0'
    testImplementation('org.junit.jupiter:junit-jupiter:5.5.1')
}

test {
    useJUnitPlatform()
}